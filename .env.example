# ───────────── PostgreSQL ─────────────
# ※ゼロ設定モードでは Postgres の環境変数は未使用です
# clone → `docker compose up -d` で認証なし(trust)起動します
# 必要に応じて手動で使う場合のみ以下を設定してください
# POSTGRES_USER=posms
# POSTGRES_PASSWORD=secure_password
# POSTGRES_DB=posms
# POSTGRES_HOST=db          # docker-compose.yml のサービス名
# POSTGRES_PORT=5432

# ───────────── Prefect OSS / Cloud ─────────────
# ローカル OSS を使う場合（docker-compose に prefect サービスを定義）
PREFECT_API_URL=http://prefect:4200/api
# Prefect Cloud を使う場合は上行をコメントアウトし、下行 2 つをセット
# PREFECT_API_URL=https://api.prefect.cloud/api/account/<ACCOUNT_ID>/workspace/<WS_ID>
# PREFECT_API_KEY=your_prefect_cloud_api_key

# ───────────── MLflow ─────────────
MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_EXPERIMENT_NAME=posms

# ───────────── アプリ設定 ─────────────
ENVIRONMENT=development
LOG_LEVEL=INFO

# ───────────── Jupyter Notebook (任意) ─────────────
JUPYTER_TOKEN=change_me
